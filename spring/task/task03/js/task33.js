/**
 * Created by kee on 2016-06-09.
 */
(function(){
    var WIDTH = 42;
    var HEIGHT = 42;
    var sText = document.getElementById('sText');
    var btn = document.getElementById("btn");

    var controller = (function(){
        function mkMaze(container,config){         //创建方块
            var x = config.x;                 //传入x参数
            var y = config.y;                //传入y参数
            var i,j;
            var el = docoument.createDocumentFragment()
        }
    })
})();